# In this one we build the PU (PrsUtil) library...
SUBDIRS = src

# These 2 dirs are just distributes for final user to play with (in
# case they get the tar ball, but not the git clone).
EXTRA_DIST = data examples